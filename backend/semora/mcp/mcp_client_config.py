"""MCP Client Configuration.

Manages client configuration settings to connect the ADK Graph agents
to the local filesystem MCP servers.
"""

from typing import Dict, Any


def get_client_config() -> Dict[str, Any]:
    """Retrieve connection config details for MCP clients.

    Returns:
        Dict[str, Any]: Configuration dictionary.
    """
    # TODO(mcp-agent): Define configurations for connecting the ADK Graph context to standard filesystem tools.
    return {}
